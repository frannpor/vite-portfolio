import { defaultLocale, portfolioContent } from "@/features/home/data/portfolio";

export const siteConfig = {
  profile: portfolioContent[defaultLocale].profile,
  navigation: portfolioContent[defaultLocale].navigation,
};
